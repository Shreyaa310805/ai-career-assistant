# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
SQLAlchemy==2.0.35
aiosqlite==0.20.0      # zero-dependency standalone DB (default)
asyncpg==0.29.0         # used when DATABASE_URL points to Postgres/Supabase
greenlet==3.1.1

# --- Resume / JD extraction ---
PyMuPDF==1.24.10        # fitz
python-docx==1.1.2

# --- LLM ---
google-genai==0.7.0      # optional: falls back to heuristic parser if no API key

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
