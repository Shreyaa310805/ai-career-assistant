"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import { AppShell } from "@/components/app-shell"; import { getSummary, type Summary } from "@/lib/applications";
const labels: Record<string, string> = { SAVED: "Saved", APPLIED: "Applied", INTERVIEWING: "Interviewing", OFFER: "Offers", REJECTED: "Closed" };
export default function Dashboard() { const [data, setData] = useState<Summary | null>(null); useEffect(() => { getSummary().then(setData).catch(() => setData(null)); }, []); const metrics = data ? [["Total roles", data.total], ["Applied", data.applied], ["Interviewing", data.interviewing], ["Offers", data.offer]] : []; return <AppShell><main className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-indigo-600">APPLICATION OVERVIEW</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Keep your search moving.</h1><p className="mt-2 text-slate-500">A clear view of every opportunity, in one place.</p></div><Link href="/applications/new" className="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-700">+ Add application</Link></div><section className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([name, value]) => <div key={name as string} className="rounded-xl border border-slate-200 bg-white p-5"><p className="text-sm text-slate-500">{name}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</section><section className="mt-8 grid gap-6 xl:grid-cols-[1.55fr_1fr]"><div className="rounded-xl border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><h2 className="font-semibold">Recent applications</h2><Link className="text-sm font-medium text-indigo-600" href="/applications">View all</Link></div>{data?.recent_applications.length ? <div>{data.recent_applications.map(app => <Link href={`/applications/${app.id}`} key={app.id} className="flex items-center justify-between border-b border-slate-100 px-5 py-4 last:border-0 hover:bg-slate-50"><span><b className="block text-sm">{app.role}</b><span className="text-sm text-slate-500">{app.company}{app.location ? ` · ${app.location}` : ""}</span></span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-600">{labels[app.status]}</span></Link>)}</div> : <div className="px-5 py-12 text-center"><p className="font-medium">No applications yet</p><p className="mt-1 text-sm text-slate-500">Add your first role to start tracking it.</p></div>}</div><div className="rounded-xl bg-slate-900 p-6 text-white"><p className="text-xs font-semibold tracking-wider text-indigo-300">WORKSPACE READY</p><h2 className="mt-2 text-xl font-bold">Every role deserves a plan.</h2><p className="mt-3 text-sm leading-6 text-slate-300">Open an application workspace to prepare your resume, interview strategy, skills and learning focus around that role.</p><Link href="/applications/new" className="mt-5 inline-block text-sm font-semibold text-indigo-300 hover:text-indigo-200">Add an opportunity →</Link></div></section></main></AppShell>; }
